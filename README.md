# El Bicho: Home Assistant

🏠 Self-hosted version of the Home Assistant for my workstation

## Requirements

- Docker-compose

## Usage

Simply run the following command.

```bash
docker-compose up -d --build
```
